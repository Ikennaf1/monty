#include "monty.h"

/**
 * main - Main entry point
 * @argc: Arg count
 * @argv: Arguments
 * Return: 1
 */
int main(int argc, char *argv[])
{
	if (argc && argv[1])
	{}

	readfile(argv[1]);

	return (0);
}
